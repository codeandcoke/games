name: games
services:
  db:
    image: mariadb:11.3.2
    container_name: games-db
    environment:
      MYSQL_USER: games_user
      MYSQL_PASSWORD: games_password
      MYSQL_DATABASE: games
      MYSQL_PORT: 3306
      MYSQL_ROOT_PASSWORD: $MARIADB_ROOT_PASSWORD
    ports:
      - "3306:3306"
#    volumes:
#      - /home/astable/mariadb_data:/var/lib/mysql
